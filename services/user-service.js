const db = require("../database/models") //must change to es6

import { Model } from 'mongoose';
import { User, UserDocument } from '../schema/user-schema';

class   UserService {

    addUser(user) {

    }

    getUser(title) {
    }

    allUser() {
    }

    updateUser(user) {
    }

    removeUser(title) {
    }

}

export default new UserService();
